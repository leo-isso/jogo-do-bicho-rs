struct Round {
    id: String,
    draws: Vec<u16>,
} // generate_draws, get_prize, get_winners

struct Gambler {}
struct Bet {}
struct BetType {} // Default, Quina, Milhar + validacoes

fn main() {
    println!("Hello, world!");
}
